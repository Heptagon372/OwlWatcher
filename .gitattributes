# 저장소 안에서는 LF 로 통일한다. Swift·Deno·CI 스크립트가 macOS·Linux 에서 돌기 때문에
# CRLF 가 섞이면 조용히 깨진다. 작업 복사본의 줄바꿈은 각자 OS 에 맞게 체크아웃된다.
* text=auto eol=lf

# 텍스트로 다루면 안 되는 것
*.png binary
*.jpg binary
*.ico binary
*.pfx binary
*.snk binary
